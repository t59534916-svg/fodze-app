node_modules/
.next/
.env.local
.env*.local
*.tsbuildinfo
.firecrawl/
.vercel/
.DS_Store
Historie/
tools/
/backtest/
!/src/app/backtest/
*.csv
*.pyc
__pycache__/
.claude/
scripts/*.json
.fodze-refresh-progress.json
matchday-*.json
spieltag*.json
backups/
*.png
!public/*.png
!public/*.jpg
.vercel

# StatsBomb Open Data cache (downloaded by tools/statsbomb/download.py)
tools/statsbomb/data/
tools/statsbomb/aggregates.csv

# FootyStats CSVs (user-downloaded, non-redistributable per ToS)
tools/footystats/csv/

# Stray scraper log
missing-tm-aliases.log

# User-local working files in repo root (PDFs, .docx, lock files)
# These are e.g. project-spec exports / planning docs the user keeps
# locally for reference but should never enter the public repo.
*.pdf
*.docx
*.html
!public/**/*.html
.~lock.*
*.tmp

# Local exploratory analyses / one-off data folders
ExakterTag/

# Auto-regenerated by Next.js — drifts between dev/prod builds.
# Tracked but always re-generated, so accidental commits add noise.
# Leave it tracked for first-time clones, just ignore deltas.
# (No gitignore entry — see scripts/refresh-all.mjs DNS-retry comment for context)
